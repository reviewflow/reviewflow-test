version: 2.1

jobs:
  install:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: yarn install

workflows:
  version: 2

  main-workflow:
    jobs:
      - install:
          filters:
            tags:
              only: /.*/
